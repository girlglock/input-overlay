[workspace]
members = [
    "common",
    "ws-server/src-tauri",
    "obs-plugin",
]
resolver = "2"

[workspace.package]
version = "1.9.0"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
strip = true
